<Project>

	<!-- Packages Usings for Hosts -->
	<ItemGroup Label="PackagesUsings" Condition="$(OlympusImplicitUsings)=='enable' And $(IsApplicationHost)">
		<Using Include="ServiceScan.SourceGenerator" />
	</ItemGroup>

</Project>