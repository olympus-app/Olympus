meta {
  name: Antiforgery
  type: http
  seq: 3
}

get {
  url: {{BasePath}}/auth/antiforgery
  body: none
  auth: none
}

script:post-response {
  const token = res.getBody().Token;
  if (token) bru.setVar("Antiforgery", token);
}
