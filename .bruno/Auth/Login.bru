meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{BasePath}}/auth/v1/login
  body: json
  auth: none
}

body:json {
  {
    "email": "{{UserName}}",
    "password": "{{UserPassword}}"
  }
}
