meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{BasePath}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "{{LoginUsername}}",
    "password": "{{LoginPassword}}"
  }
}
