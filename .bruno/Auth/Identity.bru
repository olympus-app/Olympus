meta {
  name: Identity
  type: http
  seq: 4
}

get {
  url: {{BasePath}}/auth/v1/identity
  body: none
  auth: none
}

script:post-response {
  const id = res.getBody().Id;
  if (id) bru.setVar("UserId", id);
}
