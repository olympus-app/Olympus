meta {
  name: Photo
  type: http
  seq: 7
}

put {
  url: {{BasePath}}/api/v1/core/users/photo?id={{UserId}}
  body: multipartForm
  auth: none
}

params:query {
  id: {{UserId}}
}

headers {
  If-Match: {{UserPhotoEtag}}
}

body:multipart-form {
  file: @file(C:\Sandbox\Icon.png)
}

script:post-response {
  const etag = res.getHeader("etag");
  if (etag) bru.setVar("UserPhotoEtag", etag);
}
