meta {
  name: Delete
  type: http
  seq: 5
}

delete {
  url: {{BasePath}}/api/v1/core/users/delete?id={{TestId}}&force=true
  body: none
  auth: none
}

params:query {
  id: {{TestId}}
  force: true
}

headers {
  If-Match: {{TestEtag}}
}

script:post-response {
  const etag = res.getHeader("etag");
  if (etag) bru.setVar("TestEtag", etag);
}

settings {
  encodeUrl: true
  timeout: 0
}
