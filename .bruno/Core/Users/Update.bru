meta {
  name: Update
  type: http
  seq: 4
}

put {
  url: {{BasePath}}/api/v1/core/users/:id
  body: json
  auth: none
}

params:path {
  id: {{TestId}}
}

headers {
  If-Match: {{TestEtag}}
}

body:json {
  {
    "id": "{{TestId}}",
    "name": "Éwerton Ferreira",
    "email": "ewerton2@gcgestao.com.br"
  }
}

script:post-response {
  const etag = res.getHeader("etag");
  if (etag) bru.setVar("TestEtag", etag);
}

settings {
  encodeUrl: true
  timeout: 0
}
